{
  "name": "poh-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Layered Proof-of-Humanity: Self Protocol + Semaphore reference implementation",
  "license": "MIT OR Apache-2.0",
  "engines": { "node": ">=20" },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
